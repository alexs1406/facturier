{% extends 'document/document_layout.html.twig' %}

{% block body %}
    <h1>Users list</h1>

<div class="table-responsive">
    <table class="table table-hover records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>User</th>
                <th>Role</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for roles in users.roles %}
            <tr>

                <td>{{ docType.name }}</td>
 
                <td>
                    <a class="btn btn-sm btn-info" href="{{ path('doctype_show', { 'id': docType.id }) }}">
                        <span class="glyphicon glyphicon-share"></span> Show
                    </a>

                    <a class="btn btn-sm btn-default" href="{{ path('doctype_edit', { 'id': docType.id }) }}">
                        <span class="glyphicon glyphicon-edit"></span>Edit
                    </a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
        <a href="{{ path('doctype_new') }}" class="btn btn-success">Create a new entry</a>
<div>
{% endblock %}
